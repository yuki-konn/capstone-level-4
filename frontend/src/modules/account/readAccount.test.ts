describe("readAccount function", () => {
  it("returns email, password, userName, firstName, lastName and phone when matching email and password is provided.", () => {});
  it("returns an error message when email is not in the list", () => {});
  it("returns an error message when email is in the list but password doesn't match", () => {});
  it("returns an error message when email or password is an object", () => {});
  it("returns an error message when email or password is undefined", () => {});
});
